---
interface Props{
    id?: string;
    class?: string;

}
const { id , class:className } = Astro.props;

---
<section
    id={id}
    class={`relative min-h-screen w-full flex items-center justify-center ${className ?? ""}`}
  >
  <slot/>
</section>